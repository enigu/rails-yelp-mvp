<div class="container">
  <h1>All Restaurants</h1>
  <div>
  	<% @restaurants.each do |restaurant| %>
    <p><%= link_to restaurant.name.capitalize, restaurant_path(restaurant) %></p>
    <% end %>
  </div>
</div>